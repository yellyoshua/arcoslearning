// @ts-check
const formInputForUserName = '#root > section:nth-child(2) > form > input';
const formSubmitButtonForRegister = 'text=Comenzar';

module.exports = {
	formInputForUserName,
	formSubmitButtonForRegister,
};
